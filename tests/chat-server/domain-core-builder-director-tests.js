import {expect} from 'chai';

describe("Класс DomainCoreBuilderDirector. Создает DomainCore.", () => {
    it("Хранит в себе UserAggregate.", () => {
        expect(true).is.eq(false);
    })
    it("Хранит в себе ChatAggregate.", () => {
        expect(true).is.eq(false);
    })
})